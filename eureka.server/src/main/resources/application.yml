server:
  port: 8770

spring:
  application:
    name: local-server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8770/eureka
